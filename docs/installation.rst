============
Installation
============

At the command line::

    $ easy_install langusta_client

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv langusta_client
    $ pip install langusta_client
